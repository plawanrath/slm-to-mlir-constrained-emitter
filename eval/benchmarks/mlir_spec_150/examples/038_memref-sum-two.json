{
  "id": "038_memref-sum-two",
  "dialect": "memref+arith+func",
  "difficulty": "hard",
  "nl": "Write a function that takes a memref of 2 i32 values, adds the element at index 0 and the element at index 1, and returns the sum as i32.",
  "mlir": "module {\n  func.func @sum2(%m: memref<2xi32>) -> i32 {\n    %c0 = arith.constant 0 : index\n    %c1 = arith.constant 1 : index\n    %a = memref.load %m[%c0] : memref<2xi32>\n    %b = memref.load %m[%c1] : memref<2xi32>\n    %0 = arith.addi %a, %b : i32\n    return %0 : i32\n  }\n}\n",
  "notes": "seed example; multi-op chain with memref loads and arith"
}
