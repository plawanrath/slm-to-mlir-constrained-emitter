{
  "id": "135_running-sum-3-slots",
  "dialect": "memref+arith+func",
  "difficulty": "hard",
  "nl": "Write a function that computes the sum of memref elements at indices 0, 1, 2 (memref<?xi32>) and stores it at index 3.",
  "mlir": "module {\n  func.func @rs(%m: memref<?xi32>) {\n    %c0 = arith.constant 0 : index\n    %c1 = arith.constant 1 : index\n    %c2 = arith.constant 2 : index\n    %c3 = arith.constant 3 : index\n    %a = memref.load %m[%c0] : memref<?xi32>\n    %b = memref.load %m[%c1] : memref<?xi32>\n    %s = arith.addi %a, %b : i32\n    memref.store %s, %m[%c3] : memref<?xi32>\n    return\n  }\n}\n",
  "notes": "multi-index memref reduction"
}
