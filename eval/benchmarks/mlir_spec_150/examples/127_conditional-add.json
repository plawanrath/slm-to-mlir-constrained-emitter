{
  "id": "127_conditional-add",
  "dialect": "arith+func",
  "difficulty": "medium",
  "nl": "Write a function that takes an i1 flag and two i32 values `a` and `b`; if the flag is true return `a + b`, else return `a`.",
  "mlir": "module {\n  func.func @ca(%f: i1, %a: i32, %b: i32) -> i32 {\n    %0 = arith.addi %a, %b : i32\n    %1 = arith.select %f, %0, %a : i32\n    return %1 : i32\n  }\n}\n",
  "notes": "conditional accumulator"
}
