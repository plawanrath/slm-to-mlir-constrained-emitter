{
  "id": "141_ctz-like-trailing-zero",
  "dialect": "arith+func",
  "difficulty": "medium",
  "nl": "Write a function that takes an i32 `x` and returns `x & (x - 1)` — the classic clear-lowest-set-bit bit-trick.",
  "mlir": "module {\n  func.func @cz(%x: i32) -> i32 {\n    %c1 = arith.constant 1 : i32\n    %0 = arith.subi %x, %c1 : i32\n    %1 = arith.andi %x, %0 : i32\n    return %1 : i32\n  }\n}\n",
  "notes": "bit-trick: clear lowest bit"
}
