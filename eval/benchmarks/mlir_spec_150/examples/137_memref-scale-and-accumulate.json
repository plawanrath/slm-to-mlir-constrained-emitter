{
  "id": "137_memref-scale-and-accumulate",
  "dialect": "memref+arith+func",
  "difficulty": "hard",
  "nl": "Write a function that loads f32 at index `i` from memref `m`, multiplies by `k`, adds an accumulator `acc`, and returns the new accumulator.",
  "mlir": "module {\n  func.func @ma(%m: memref<?xf32>, %i: index, %k: f32, %acc: f32) -> f32 {\n    %0 = memref.load %m[%i] : memref<?xf32>\n    %1 = arith.mulf %0, %k : f32\n    %2 = arith.addf %acc, %1 : f32\n    return %2 : f32\n  }\n}\n",
  "notes": "load-scale-accumulate"
}
