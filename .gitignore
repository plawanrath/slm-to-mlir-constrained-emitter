# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
*.egg-info/
dist/
build/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
.hypothesis/

# ML / experiment tracking
data/raw/
data/processed/
checkpoints/
models/
wandb/
mlruns/
runs/
tensorboard/
lightning_logs/
ray_results/
outputs/
multirun/
.hydra/

# Model weight formats (keep pointers only)
*.safetensors
*.bin
*.gguf
*.pt
*.pth
*.ckpt
*.h5
*.hdf5
*.pkl
*.pickle
*.onnx
*.tflite
*.npz
*.npy

# Run results / caches (keep aggregated CSVs/JSONs needed for figures via `git add -f`)
results/
*.sqlite
*.sqlite3
*.db

# Logs
*.log
logs/

# Notebooks
.ipynb_checkpoints/

# macOS
.DS_Store
**/.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local agent / Claude state
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/cache/

# Paper / LaTeX build artifacts
docs/paper/*.aux
docs/paper/*.bbl
docs/paper/*.blg
docs/paper/*.log
docs/paper/*.out
docs/paper/*.synctex.gz
docs/paper/*.toc
docs/paper/*.fdb_latexmk
docs/paper/*.fls
docs/paper/*.lof
docs/paper/*.lot
docs/paper/*.nav
docs/paper/*.snm
docs/paper/*.vrb
docs/paper/*.bcf
docs/paper/*.run.xml
docs/paper/*.idx
docs/paper/*.ilg
docs/paper/*.ind
docs/design.md
# Personal / contact artifacts (do not publish on the public repo)
RESUME.md
CONTACT.md

# Paper / submission build outputs (regenerate locally; upload to OpenReview/Zenodo)
docs/paper/
docs/daily_log/
submission_artifact/
overleaf_upload.zip
*.tar.gz
!eval/**/*.tar.gz
!data/pipelines/**/*.tar.gz

# Paper-process / camera-ready edit notes (internal; never publish)
CHANGELOG_for_camera_ready.md
CHANGELOG_*camera_ready*.md
*_camera_ready.md
*_camera_ready_notes.md

# Submission-artifact build tooling (process-internal; not redistributed)
scripts/build_submission_artifact.sh

# HuggingFace dataset upload staging area (regenerated by scripts/build_hf_uploads.py)
hf_uploads/
scripts/build_hf_uploads.py
scripts/upload_hf_datasets.py

# Camera-ready URL substitution log (process-internal; do not publish)
# Implicitly covered by `docs/paper/` above; explicit entry for clarity.
docs/paper/artifacts_links.md

# Internal planning docs (work-log, execution plan, scratch)
# These are tracked historically; new copies will not be tracked. To stop
# tracking the existing files, run `git rm --cached <path>` separately.
TODO.md
docs/execution_plan.md
NOTES.md
SCRATCH.md
scratch/
wip/
*.draft.md

# Misc OS / editor artifacts
Thumbs.db
*.bak
*.tmp
*~
.history/
.aider*
.cursor/
